import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("MorePlay")
                .font(.largeTitle)
            
            Text("CarPlay Extension Active")
        }
    }
}
